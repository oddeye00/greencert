# Frozen cancellation-safe corrected-path audit protocol

Frozen on 2026-08-29 before the new Gaussian block was instantiated.

## Motivation fixed by the preceding audit

The v1 corrected-path audit was frozen before its probe and used the literal
float64 subtraction `G(c+d)-(c+d)_next`.  It abstained because that subtraction
had norm `3.101357457947468e-15`, while prior cancellation-safe and 192-bit
audits had already established that the amplified second-order forcing is many
orders smaller.  V1 is retained as a failed numerical interface.  V2 changes
only that interface; it does not tune the candidate, horizon, amplification,
probe count, Gram power, failure allocation, derivative envelope, or event.

## Fixed construction

- Candidate: seed 366, 70% gate, anchor 1040.
- Horizon: 52; four sealed variational sweeps.
- Corrected path: `b=c+d`, where `d=K_Hs` is the complete signed response and
  `d_0=0`.
- Queried operator: the causal Green operator generated by `DG(b_j)`.
- Green probes/power: 16 probes, power one.
- Per-operator failure allocation: `4.59896983075791e-11`.
- Corrected forcing: fixed `lambda=4096` amplified secant, using the previously
  frozen analytic discrepancy and the independently recomputed 204-interval
  192-bit forcing-norm upper bound.
- Closure: `Y + kappa_bar*M*E^2/2 <= E`; the mixed coefficient is exactly zero.
- Output transport: the original power-one output bounds and corrected-center
  margin formula for the same persistent 70% event.
- No revealed future trajectory may be read.

## Fresh randomized query

- Master nonce:
  `8d6d17bfe5fb4a861d7673d13a633bfe6e761b0e6755a3d21030c5dea0992066`.
- Operator identity: `(92,366,0,1040,52,4,1)`.
- All scientific and arithmetic choices above precede generation of the
  nonce-derived probe block.

## Frozen hashes

- `scripts/audit_transformer_relinearized_secant.py`:
  `D6459D702F62AF11427FA26E197AA0C8F5D6D2EFEFC1C05C36FEA1EB0E90C8F6`
- `scripts/relinearized_green_closure.py`:
  `ACFB21152D0467E6DDAAB627138325B2079C4F75C1F8BD7FF708C427F67FE5E1`
- `RELINEARIZED_GREEN_THEOREM.md`:
  `2E4928E4894B50FBD583B69596828ED34B4B48CA7A644B0960531E785C4F05F3`
- Amplified-secant geometry result:
  `029E364FC6D773F9BD1BCFA7444F7879A1DACF918268B354A89F38B0791807FF`
- Independent 204-interval result:
  `C05C3C274AC12B17FB1EC992752981CB55986F75C23B13783C48EEFE52ABEBE2`
- Failed v1 corrected-path result:
  `9E90C3F53F66704699A8F3F7ADEB05B92311952D2DD6ED1C8BBAF60C909B6633`

This remains post-seal method development.  The amplified scalar forcing is
outward conditional on stored dyadic inputs, but the rebuilt Green products
and output margins remain float64/high-confidence rather than a complete
computer-assisted proof.
